<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.castelaofpe.helptech">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".principal.PreguntaDetalleActivity"></activity>
        <activity android:name=".inicio.PreferenciasActivity" />
        <activity android:name=".PruebaActivity" />
        <activity android:name=".principal.MainActivity"/>
        <activity android:name=".perfil.PerfilActivity" />
        <activity android:name=".perfil.MensajesActivity" />
        <activity android:name=".inicio.InicialActivity"/>
        <activity android:name=".inicio.PreinicialActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>